import React from 'react';
import { Button } from 'react-bootstrap';
import './ServicesMenu.scss';

interface ServicesMenuProps {
    onClick: any;
}

export const ServicesMenu = (props: ServicesMenuProps): JSX.Element => {
    return (
        <div className="ServicesMenu">
            <div className="left-part">qwe</div>
            <div className="right-part">qwe</div>
        </div>
    );
};
